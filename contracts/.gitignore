# Compiler artifacts
cache/
out/

# Deploy broadcasts (kept locally, not committed · contains tx
# transcripts and timestamps from prior runs)
broadcast/

# Forge dependencies (run `forge install` after clone)
lib/

# Dotenv file
.env
